import { SelectProductPipe } from './select-product.pipe';

describe('SelectProductPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectProductPipe();
    expect(pipe).toBeTruthy();
  });
});
